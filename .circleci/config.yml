version: 2.1

orbs:
  node: circleci/node@4.2.0
jobs:
  install-node-example:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: false
      - run: node --version
      - node/install-packages
workflows:
  test-my-app:
    jobs:
      - install-node-example
